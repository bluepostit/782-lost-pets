<h1>Found Pets</h1>
<p>Recently found pets. Help us get them home!</p>

<ul>
  <% @pets.each do |pet| %>
    <li>
      <%= link_to "#{pet.color} #{pet.species}", pet_path(pet) %>, found <%= pet.found_days_ago %> days ago

    </li>
  <% end %>
</ul>
